 Mermaid diagram - S3 and SageMaker in the AWS Infrastructure box:

```mermaid
flowchart TD
    A[(MovieLens 33.8M ratings)] --> B[Data Pipeline]
    B --> C[Trend Forecasting\nLightGBM]
    B --> D[Recommendation Engine\nNCF + BPR]
    B --> E[RL Optimization\nLinUCB Bandit]
    C --> F[Merge Signals]
    D --> F
    E --> F
    F --> G[LLM Explanation Layer\nClaude API]
    G --> H((User))
    H --> I[reward / engagement signal]
    I --> E

    subgraph AWS Infrastructure
        J[SageMaker\nNCF Training]
        K[(S3\nData & Model Artifacts)]
    end

    D -.-> J
    J -.-> K
    C -.-> K
    E -.-> K
```

Click the pencil icon on the README, find the existing Mermaid block, replace it entirely with this. Then commit.
